# PRad2 Replay Configuration File
# Key/value pairs are separated by white space
# Lines starting with # and empty lines are ignored

# SCICOMP JOB ACCOUNTING
USER                          acschick
PROJECT                       hallb                   # SLURM account
TRACK                         production              # SLURM partition (debug, production, etc.)
OS                            el9

# JOB RESOURCES
NCORES                        1                      # Number of cores (also used for -j threads parameter)
DISK                          4GB                    # Disk space per job
RAM                           2GB                    # RAM per job
TIMELIMIT                     2hrs                    # Time limit per job

# WORKFLOW DEFINITION
WORKFLOW                      prad2_replay_test       # SWIF2 workflow name

# ENVIRONMENT SETUP
ENVFILE                       /work/hallb/prad/acschick/swif2-replayer/set_env_CUE_modern.csh

# REPLAY EXECUTABLE
REPLAY_EXEC                   /work/hallb/prad/acschick/prad2evviewer/build/bin/prad2ana_replay_recon

# REPLAY PARAMETERS
ZVERTEX_CUT                   5                       # -z parameter

# FILE INPUT/OUTPUT DIRECTORIES
# Data location - use /mss/... for tape (SWIF2 handles jcache automatically)
#                 or use /cache/... or /lustre24/... if already on disk
INDATA_TOPDIR                 /mss/clas12/rg-o/data

# Output directories
OUTDIR_LARGE                  /volatile/hallb/prad/acschick/PRad2/replay_output
OUTDIR_SMALL                  /farm_out/acschick/prad2/replay          # For log files
